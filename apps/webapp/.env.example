# In dev, leave empty to use Vite proxy at /api. Set to the public API origin in prod builds.
VITE_API_URL=
# If your backend runs on a non-default port in dev, set Vite's proxy target here.
VITE_API_PROXY=http://localhost:3000
