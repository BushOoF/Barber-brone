# Pinned runtime dependencies for the barber voice AI sidecar (Python 3.11).
# Web framework + ASGI server
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Speech-to-text (CTranslate2 backend)
faster-whisper==1.1.1

# HTTP client for Ollama
httpx==0.28.1

# Validation / schemas
pydantic==2.10.5

# Env loading
python-dotenv==1.0.1

# Multipart form parsing for the /process-voice upload
python-multipart==0.0.20
